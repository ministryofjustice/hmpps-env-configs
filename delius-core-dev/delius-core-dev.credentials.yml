---

environment_name: delius-core-dev
project: delius-core
account: "{{ project }}"

delegated_account_id: "723123699647"
delegated_role: "admin"
# environment_name / account / product / service / cred_name
# env / account / product / service / cred_name

# environment_name / account     / products.name   / namespaces.name  / cred_name
# environment_name / project     / products.name   / service          / cred_name
# /playpit         / delius-core / role-name       / service//host    / cred_name//var_name

# /playpit         / delius-core / weblogic        / weblogic         / wls_admin_user
# /playpit         / delius-core / oracle-database / db               / database_password_all


products:
- name: apacheds
  namespaces:
  - name: apacheds
    roles:
    - creds:
      - name: ldap_admin_password
        type: password
        value: secret
        account_id: "{{ delegated_account_id }}"
        role_name: "terraform"
        is_delegated: True
      - name: test_user_password
        type: password
        value: Password1
        account_id: "{{ delegated_account_id }}"
        role_name: "terraform"
        is_delegated: True
- name: weblogic
  namespaces:
  - name: ndelius-domain
    roles:
    - creds:
      - name: weblogic_admin_password
        type: password
        value: webl0gic
        account_id: "{{ delegated_account_id }}"
        role_name: "terraform"
        is_delegated: True
      - name: nomis_client_secret
        type: password
        value: ThisIsASecretKey_key
        account_id: "{{ delegated_account_id }}"
        role_name: "terraform"
        is_delegated: True
      - name: newtech_secret_key
        type: password
        value: ThisIsASecretKey
        account_id: "{{ delegated_account_id }}"
        role_name: "terraform"
        is_delegated: True
      - name: usermanagement_secret_key
        type: password
        value: ThisIsASecretKey
        account_id: "{{ delegated_account_id }}"
        role_name: "terraform"
        is_delegated: True
  - name: interface-domain
    roles:
    - creds:
      - name: weblogic_admin_password
        type: password
        value: webl0gic
        account_id: "{{ delegated_account_id }}"
        role_name: "terraform"
        is_delegated: True
      - name: offloc_password
        type: password
        value: Password1
        account_id: "{{ delegated_account_id }}"
        role_name: "terraform"
        is_delegated: True
      - name: ndelius_dss_password
        type: password
        value: Password1
        account_id: "{{ delegated_account_id }}"
        role_name: "terraform"
        is_delegated: True
      - name: encryption_iv
        type: password
        password_length: 16
        account_id: "{{ delegated_account_id }}"
        role_name: "terraform"
        is_delegated: True
  - name: spg-domain
    roles:
    - creds:
      - name: weblogic_admin_password
        type: password
        value: webl0gic
        account_id: "{{ delegated_account_id }}"
        role_name: "terraform"
        is_delegated: True
      - name: remote_broker_username
        type: string
        value: spgsmx
        account_id: "{{ delegated_account_id }}"
        role_name: "terraform"
        is_delegated: True
      - name: remote_broker_password
        type: password
        value: spgsmx1000000
        account_id: "{{ delegated_account_id }}"
        role_name: "terraform"
        is_delegated: True
- name: oracle-database
  namespaces:
  - name: db
    roles:
    - creds:
      - name: delius_app_schema_password
        type: password
        value: NDelius1
        account_id: "{{ delegated_account_id }}"
        role_name: "terraform"
        is_delegated: True
      - name: delius_pool_password
        type: password
        value: NDelius1
        account_id: "{{ delegated_account_id }}"
        role_name: "terraform"
        is_delegated: True
      - name: system_password
        type: password
        value: NDAmanager1
        account_id: "{{ delegated_account_id }}"
        role_name: "terraform"
        is_delegated: True
